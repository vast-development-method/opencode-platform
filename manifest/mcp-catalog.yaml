schema: 1
reviewed_on: 2026-07-22
servers:
  git:
    status: enabled
    trust: model-context-protocol-project
    transport: stdio
    source: https://github.com/modelcontextprotocol/servers/tree/main/src/git
    package: mcp-server-git
    note: Early development; restrict repository path to /workspace.
  github:
    status: ready-disabled
    trust: first-party
    transport: remote-or-stdio
    source: https://github.com/github/github-mcp-server
    default_remote_url: https://api.githubcopilot.com/mcp/
    note: Enable only selected toolsets and use a short-lived or fine-grained identity.
  gitea:
    status: ready-disabled
    trust: first-party
    transport: remote-or-stdio
    source: https://gitea.com/gitea/gitea-mcp
    default_authority: https://git.vdm.dev
    note: Preserve this integration; pin an audited release before enabling it in company images.
  nextcloud:
    status: candidate-disabled
    trust: community
    transport: remote-preferred
    source: https://github.com/cbcoutinho/nextcloud-mcp-server
    default_authority: https://nextcloud.example.invalid
    note: Audit and pin before adoption.
  playwright:
    status: enabled-on-web-images
    trust: first-party
    transport: stdio
    source: https://github.com/microsoft/playwright-mcp
    package: "@playwright/mcp"
    note: Prefer deterministic Playwright tests for suites; keep MCP for exploratory QA.
  joomla:
    status: internal-unfinished
    trust: internal
    transport: remote-preferred
    source: https://github.com/vast-development-method/joomla-mcp
    note: This is the only approved Joomla MCP source. Keep disabled until its first reviewed release.
  jcb:
    status: internal-unfinished
    trust: internal
    transport: remote-preferred
    source: pending
    note: Keep disabled until the VDM JCB MCP repository and first approved release are available.
  speech_to_text:
    status: optional
    trust: internal-endpoint
    transport: command-or-remote
    source: internal OpenAI-compatible transcription endpoint
    note: Host microphone capture remains outside the VM; inject only transcribed text.
